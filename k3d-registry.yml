mirrors:
  local.registry.localhost:5000:
    endpoint:
    - "http://local.registry.localhost:5000"

  docker.io:
    endpoint:
    - "http://docker.mirror.registry.localhost:5000"

  quay.io:
    endpoint:
    - "http://quay.mirror.registry.localhost:5000"

  registry.k8s.io:
    endpoint:
    - "http://k8s-community.mirror.registry.localhost:5000"
